{
  "name": "reach-pack",
  "version": "1.0.0",
  "description": "Put VR within Reach",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack --config webpack.config.js",
	"devserver": "http-server -p 8000 --cors src/",
	"twineserver": "http-server -p 8000 --cors dist/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^0.11.1",
    "webpack": "^4.41.2",
    "webpack-cli": "^3.3.10"
  },
  "dependencies": {
    "aframe": "^0.9.2"
  }
}
